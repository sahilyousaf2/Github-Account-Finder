export type EventType = {
    target: {
        value: string,
        name: string
    }
}

export type DataType = {
    login: string,
    avatar_url: string,
    bio: string,
    followers: number,
    following: number
}